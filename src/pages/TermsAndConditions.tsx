import LegalDocumentPage from "@/components/LegalDocumentPage";
import { termsContent } from "@/data/siteContent";

const TermsAndConditions = () => <LegalDocumentPage document={termsContent} />;

export default TermsAndConditions;
